<!--
整份 PR 描述会直接成为 GitHub Release 正文，无需 release-notes 标记。
The entire PR description becomes the GitHub Release body; no release-notes markers are needed.
请填写下面的中英文更新说明，删除不适用的章节；验证记录等评审信息请放到 PR 评论中。
Complete the bilingual release notes below and remove unused sections. Put validation and review details in PR comments.
不设置发布标签等同于 release:none，只合并不发布。发布渠道选择 release:stable 或 release:preview；补发与 main 相同的版本时额外添加 release:retry。
No release label means release:none (merge without publishing). Select release:stable or release:preview to publish; additionally apply release:retry to publish the same version as main.
发布时项目版本必须高于目标分支，工作流不会自动递增版本。详见 .github/RELEASING.md。
Publishing requires a project version greater than the base version; the workflow does not increment it automatically.
-->

## 新增：

1.

## New Features:

1.

## 修复：

1.

## Fixes:

1.
